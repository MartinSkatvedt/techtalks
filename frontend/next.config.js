/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = {
  nextConfig,
  env: {
    sanityProjectId: "pmrju0ye",
    sanityDataset: "production",
  },
};
